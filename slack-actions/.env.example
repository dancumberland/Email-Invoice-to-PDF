# DCL Actions listener — secrets. Real file lives at /home/claude/slack-actions/.env
# on the VPS, chmod 600, claude-owned. NEVER commit the real .env.
# Format is plain KEY=value (valid for both systemd EnvironmentFile and our parser):
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=
DAN_USER_ID=
DECISIONS_CHANNEL_ID=
