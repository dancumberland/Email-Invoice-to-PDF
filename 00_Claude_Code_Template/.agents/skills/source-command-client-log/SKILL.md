---
name: source-command-client-log
description: "Run the canonical Claude /client-log command for 00_Claude_Code_Template. Use when the user invokes /client-log or asks for log session."
---

<!-- generated by sync_claude_project_capabilities.py -->

# Source command: /client-log

Before taking action, read
[`../../../.claude/commands/client-log.md`](../../../.claude/commands/client-log.md)
completely. That Claude command is canonical.

Follow it faithfully, including its argument handling. Adapt Claude-specific
tool names to the closest available Codex tools while preserving its
authorization gates, confirmation steps, write boundaries, and stop
conditions. Resolve source-relative paths from the project root.

If the source calls a legacy `.claude/workflows/*.js` runner, treat that section
as the workflow specification. Use Codex collaboration agents for the bounded
work the command explicitly authorizes, keep the main agent as the sole writer,
and preserve the source command's concurrency and permission limits.
